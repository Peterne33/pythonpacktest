[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "pythonpacktest"
version = "0.1.0"
description = "My first package"
readme = "README.md"
requires-python = ">=3.8"
authors = [
  { name="Your Name" }
]
